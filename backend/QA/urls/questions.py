# from django.urls import path, re_path
# from QA import views
#
# urlpatterns = [
#     re_path('^$', ),
#     path('<int:pk>/', ),
#     path('watch/', ),
#     path('cancel_watch/', ),
#     path('search/<info>/start-<int>/counts-<int, default=20>/', ),
#     path('page-<int:page>default=0>/counts-<int,default=20>/topic-<topic_key>/', )
# ]
