# from django.urls import path
# from QA import views
#
# urlpatterns = [
#     path('/', ),
#     path('page-<int:page>default=0>/counts-<int,default=20>/', ),
#     path('topic-<int:id>/start-<int>/count-<int>/', ),
#     path('search/<string: info>/start-<int>/counts-<int, default=20>/', )
# ]
